"""Pure computation: no Qt, no UI, no global state.

Everything here takes its inputs as arguments and returns plain values, so the
same code runs under the app, under ``tools/batch_run.py`` and under tests.
"""
