# IMTOP wound annotator: Python dependencies. Python 3.11, 64-bit Windows.
# Versions are pinned to the validated environment. Only what the shipped app
# imports is listed; the analysis scripts in tools/ have their own extra list
# (tools/requirements-analysis.txt).
#
# PyTorch is listed without a build tag on purpose. The installer picks the
# wheel index (CPU by default, CUDA 12.4 when it finds an NVIDIA GPU), installs
# torch and torchvision first, and this file then finds them satisfied.
# Installing by hand, CPU build:
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# Swap /cpu for /cu124 to use an NVIDIA GPU. Skipping the first line also
# works: on Windows the PyPI torch wheel is the CPU build.

# --- core: arrays, geometry, image I/O and CV ---
numpy==1.26.4
scipy==1.13.1
opencv-python==4.11.0.86
pillow==12.2.0

# --- GUI: Qt + the embedded Chromium web view ---
# The app prefers PyQt6 and falls back to PyQt5 + PyQtWebEngine if PyQt6 is absent.
PyQt6==6.11.0
PyQt6-WebEngine==6.11.0

# --- the window and the look: SlantUI ---
# The frameless window, the oblique title bar, the stylesheets and the widget
# scripts are a library of their own, not part of this application. IMTOP is
# its first user; it imports it like any other dependency and keeps none of it
# in its own tree.
#
# On the index since 2026-09-18, so this is an ordinary pin and the installer
# needs nothing new. A development machine that works on both at once still
# does pip install -e <path to SlantUI>, which takes precedence over this.
# The shell extra is left off on purpose: PyQt6 and the WebEngine are pinned
# above, at the versions this application is tested on, and the extra would
# ask for them a second time with a looser bound.
slantui==0.3.0

# --- Segment Anything (SAM, ViT-B) back-end ---
torch==2.6.0
torchvision==0.21.0
segment-anything==1.0
